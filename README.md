# Animal Diversity Prediction

## Setup

Setup environment and install dependencies

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Go to the `notebooks` directory and run them in the folder.
